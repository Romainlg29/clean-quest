services:
  postgis:
    image: postgis/postgis:18-3.6-alpine
    container_name: cleanquest_postgis
    environment:
      POSTGRES_USER: cleanquest
      POSTGRES_PASSWORD: jJyTIs0WmGM+J8TV0fIrajQva6DgeQ2UctGLFFheDxo=
      POSTGRES_DB: cleanquest
    ports:
      - "5432:5432"
    volumes:
      - ./.volumes/postgis:/var/lib/postgresql
    restart: unless-stopped

  # nginx:
  #   image: nginx:alpine
  #   container_name: cleanquest_nginx
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - ./dev.nginx.conf:/etc/nginx/conf.d/default.conf:ro
  #   extra_hosts:
  #     - "host.docker.internal:host-gateway"
  #   restart: unless-stopped
